<%= form_for @list do |f| %>

	<%= f.label :title %>
	<%= f.text_field :title %>

	<%= f.label :description %>
	<%= f.text_field :description %>

  <div class="btn waves-effect waves-light">
  	<%= f.submit %>
  </div>
<% end %>
