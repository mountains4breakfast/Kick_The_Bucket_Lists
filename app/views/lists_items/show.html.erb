<div class="row">
  <div class="col s10 offset-s1 m4 small card hoverable teal lighten-3">
    <p><%= @list.title %></p>
    <p><%= @item.title %></p>
    <p>Completed: <%= @lists_item.completed %></p>
    <%= link_to 'Adjust status', complete_path(@lists_item), method: :put %>
  </div>
</div>
